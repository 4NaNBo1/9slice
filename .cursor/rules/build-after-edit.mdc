<!-- generated by sync-rules -->
---
description: "完成一轮代码、配置或构建脚本修改后必须统一执行构建"
globs: src/**/*.ts,*.json,*.js
alwaysApply: false
---

# 方案完成后统一构建


完成一轮代码、配置或构建脚本修改后，必须统一执行一次构建，确认编译通过后再交付或进入下一轮方案。

1. 完成本轮修改
2. 运行 `npm run build`
3. 构建成功后交付或继续下一轮方案
4. 构建失败时先修复编译错误，再回到步骤 2

共享算法变更还必须运行 `npm test`。平台类型或 typings 相关变更还必须运行 `npm run typecheck`。
